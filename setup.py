"""
PDEForge: A unified framework for generating PDE datasets for operator learning.

This setup.py is provided for backwards compatibility.
The recommended way to install is via pyproject.toml:
    pip install .
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
